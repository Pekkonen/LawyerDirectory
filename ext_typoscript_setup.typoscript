# Remove that part while removing TYPO3 9 compatibility
config.tx_extbase {
  persistence{
    classes {
      Krable\LawyerDirectory\Domain\Model\Category {
        mapping {
          tableName = sys_category
        }
      }
    }
  }
}
